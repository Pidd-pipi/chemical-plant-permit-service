const rows=document.querySelector('#rows'),message=document.querySelector('#message');async function load(){const r=await fetch('/api/v1/permits');const d=await r.json();rows.innerHTML=d.items.map(x=>`<tr><td>${x.id}</td><td>${x.facility}</td><td>${x.operation}</td><td>${x.status}</td><td>${x.risk_level}</td><td><button data-id="${x.id}">暂停许可</button></td></tr>`).join('');rows.querySelectorAll('button').forEach(b=>b.onclick=()=>update(b.dataset.id))}async function update(id){const r=await fetch(`/api/v1/permits/${id}/status`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({status:'suspended'})});message.textContent=r.ok?'许可状态已更新':'更新失败';if(r.ok)load()}document.querySelector('#refresh').onclick=load;load().catch(()=>message.textContent='无法读取许可记录');
